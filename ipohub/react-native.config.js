module.exports = {
  // Copies the icon fonts into the native projects — react-native-vector-icons
  // needs them registered, otherwise every icon renders as a missing glyph.
  assets: ['./node_modules/react-native-vector-icons/Fonts'],
};
